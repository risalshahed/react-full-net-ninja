// fetch data from json server
// say, db.json file is in the following directory
// react-projects/data/db.json
// we'll display the data on "PORT 4000"
// npx json-server --watch data/db.json --port 4000
// hence, the generalize command is
// npx json-server --watch "Location of JSON file" --port "PORT NUMBER"
// hence the URL of local server is
// http://localhost:4000/blogs